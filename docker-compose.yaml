version: '3.9'

services:
    verify:
        build: .
        ports:
            - '8080:8080'
        volumes:
            - ./configs/config.yaml:/etc/cealgull-verify/config.yaml
        networks:
            - verify-network
    keydb:
        image: eqalpha/keydb
        networks:
            - verify-network
        hostname: keydb.cealgull.verify
networks:
    verify-network:
